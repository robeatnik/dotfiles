set -g default-terminal "screen-256color"
set -g prefix C-s
unbind C-b
setw -g mode-keys vi

#选择分割的窗格，不需要松开ctrl键，多次跳转方便
bind C-k selectp -U #上面panel
bind C-j selectp -D
bind C-h selectp -L
bind C-l selectp -R
